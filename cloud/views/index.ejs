<!DOCTYPE html>
<html>
	<head>
		<% include layouts/head.ejs %>
	</head>
	<body>
		<% include layouts/header.ejs %>
		Welcome	
		<% if(user !== 'undefined'){ %>
			<%=  user.forEach(sUser){ %>
				<%= sUser.username  %>
				<%= sUser.password  %>
			<% } %>
		<% } else {%>
			user undefined!
		<% } %>	
<hr >
		<% if(msg !== 'undefined'){ %>
			<%=  msg.forEach(sUser){ %>
				<%= sUser.username  %>
				<%= sUser.password  %>
			<% } %>
		<% } else {%>
			msg undefined!
		<% } %>		
<hr >
		<% include layouts/footer.ejs %>
	</body>
</html>